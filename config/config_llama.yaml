# docker run -p 8080:8080 -v /path/to/models:/models --gpus all ghcr.io/ggml-org/llama.cpp:server-cuda -m models/7B/ggml-model.gguf -c 512 --host 0.0.0.0 --port 8080 --n-gpu-layers 99


healthCheckTimeout: 500

logLevel: debug

# note: quite entertaining, but loading states impact further generation
# sendLoadingState: true

apiKeys:
  - "sk-d2V0bmF0dXJhbGx5bHVuZ3NwYXJ0aWN1bGFyc2ltcGxlZnV0dXJlbGVuZ3RocG9z"

models:
  gemma-3-4b-it:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "google/gemma-3-4b-it"
    cmd: >
      docker run
      --runtime nvidia  
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/gemma-3-4b-it-Q4_K_M.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  gemma-3-12b-it:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "google/gemma-3-12b-it"
    cmd: >
      docker run
      --runtime nvidia  
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/gemma-3-12b-it-Q4_K_M.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  gemma-4-12b-it:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "google/gemma-4-12b-it"
    cmd: >
      docker run
      --runtime nvidia  
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/gemma-4-12b-it-Q4_K_M.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  gpt-oss-20b:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "openai/gpt-oss-20b"
    cmd: >
      docker run
      --runtime nvidia  
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/gpt-oss-20b-Q4_K_M.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  sw3-6.7b-v2-instruct:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "AI-Sweden-Models/gpt-sw3-6.7b-v2-instruct"
    cmd: >
      docker run 
      --runtime nvidia 
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/gpt-sw3-6.7b-v2-instruct-Q4_K_M.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  sw3-20b-instruct:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "AI-Sweden-Models/gpt-sw3-20b-instruct"
    cmd: >
      docker run 
      --runtime nvidia 
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/gpt-sw3-20b-instruct-Q4_K_M.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  granite-guardian-3-8b:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "ibm-granite/granite-guardian-3.0-8b"
    cmd: >
      docker run 
      --runtime nvidia 
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/granite-guardian-3.0-8b-q4_k_m.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  llama-guard-3-8b:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "meta-llama/Llama-Guard-3-8B"
    cmd: >
      docker run 
      --runtime nvidia 
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/llama-guard-3-8b-q4_k_m.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  ministral-3-14b-instruct-2512:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "mistralai/Ministral-3-14B-Instruct-2512"
    cmd: >
      docker run 
      --runtime nvidia 
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/Ministral-3-14B-Instruct-2512-Q4_K_M.gguf
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512
  mistral-7b-instruct:
    proxy: "http://host.docker.internal:${PORT}"
    useModelName: "mistralai/Mistral-7B-Instruct-v0.3"
    cmd: >
      docker run 
      --runtime nvidia 
      --gpus all
      --name ${MODEL_ID}
      --rm
      -p ${PORT}:5800
      -v C:/Users/albin/Documents/Projects/llm/models:/models
      ghcr.io/ggml-org/llama.cpp:server-cuda
      -m /models/bulk/Mistral-7B-Instruct-v0.3.Q4_K_M.gguf"
      --port 5800
      --jinja
    cmdStop: docker stop ${MODEL_ID}
      # -c 512